import { WebSocketListener } from './web-socket-listener';

describe('WebSocketListener', () => {
  it('should create an instance', () => {
    expect(new WebSocketListener()).toBeTruthy();
  });
});
